PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=pets
DB_SSLMODE=disable

# dev: true (GORM AutoMigrate). prod: false (use `make migrate-up`).
AUTO_MIGRATE=true

# JWT auth — change secret in production.
JWT_SECRET=dev-secret-change-me
JWT_EXPIRY_HOURS=24
